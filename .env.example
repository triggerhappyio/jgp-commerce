# Stripe — https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
# Stripe webhook signing secret — from `stripe listen` locally, or from
# the webhook's settings page in the Stripe dashboard once deployed.
STRIPE_WEBHOOK_SECRET=whsec_...

# Postgres connection string — from Neon or Supabase's dashboard.
DATABASE_URL=postgresql://user:password@host:5432/jgp
